<h1>Producto</h1>

<ul>
        <li>
        <p>{{product.title}}</p>
        <p>{{product.description}}</p>
        <p>{{product.code}}</p>
        <p>{{product.price}}</p>
        <p>{{product.status}}</p>
        <p>{{product.stock}}</p>
        <p>{{product.category}}</p>
        <p>{{product.thumbnails}}</p>
        </li>
</ul>
